# General settings
# ----------------------------------------------------------------------------
set -g mouse on
# Number windows starting at 1
set -g base-index 1
# Keep window numbers contiguous after one is closed
set -g renumber-windows on
# Number panes starting at 1
setw -g pane-base-index 1
# Faster ESC detection
set -sg escape-time 10

# Better copy mode with system clipboard
setw -g mode-keys vi
# Note: Requires xclip
bind -T copy-mode-vi y send-keys -X copy-pipe-and-c "xclip -selection clipboard -in"

set -g default-terminal "tmux-256color"
set -as terminal-overrides ",xterm-256color:RGB"

# Pass extended keys (e.g. Shift+Enter) through to the inner program.
# Requires tmux 3.2+. Kitty sends CSI 13;2u for Shift+Enter (see kitty.conf).
set -s extended-keys on
set -as terminal-features 'xterm*:extkeys'
# ----------------------------------------------------------------------------


# Plugins
# ----------------------------------------------------------------------------
set -g @plugin 'tmux-plugins/tpm'
# Sensible has the following settings by default:
# set -s escape-time 0
# set -g history-limit 50000
# set -g display-time 4000
# set -g status-interval 5
# set -g default-command "reattach-to-user-namespace -l $SHELL"
# set -g default-terminal "screen-256color"
# set -g status-keys emacs
# set -g focus-events on
# setw -g aggressive-resize on
# set -g @plugin 'tmux-plugins/tmux-sensible'
# bind C-p previous-window
# bind C-n next-window
# bind R source-file '~/.tmux.conf'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-pain-control'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
# Auto-restore the last saved session when a fresh tmux server starts,
# and auto-save in the background every 15 minutes.
set -g @continuum-restore 'on'
set -g @continuum-save-interval '15'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'catppuccin/tmux#v2.1.3'

# Catppuccin settings
set -g @catppuccin_flavor "frappe"
set -g @catppuccin_pane_status_enabled "yes"
set -g @catppuccin_pane_border_status "yes"
set -g @catppuccin_window_status_style "rounded"
set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_window_current_text "#W"
set -g @catppuccin_window_text "#W"

# Set status bar
set -g status-right-length 100
set -g status-left-length 100
set -g status-left ""
set -g status-right "#{E:@catppuccin_status_application}"
set -ag status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_uptime}"
set -agF status-right "#{E:@catppuccin_status_cpu}"
set -agF status-right "#{E:@catppuccin_status_battery}"
# ----------------------------------------------------------------------------

# Initialize TPM (Needs to be at the very bottom of .tmux.conf)
run '~/.tmux/plugins/tpm/tpm'


# Bindings (Added at the end so that they can override plugin defaults)
# ----------------------------------------------------------------------------

# Panel navigation shortcuts are set through tmux-pain-protocol
# Moving between panes: Prefix + h or C-h, etc
# Resizing panes: Prefix + H, etc

# Override keymaps for moving between windows.
bind -n M-p previous-window
bind -n M-n next-window

# Kill window
bind -n M-w kill-window

# New window with rename prompt
# (-a inserts it right after the current window instead of at the end, and
# also make sure that it opens in the current directory to preserve tmux-pain-control's behavior)
bind c command-prompt -p "Window name:" "new-window -a -c '#{pane_current_path}' -n '%%'"

# Floating lazygit popup scoped to the current pane's directory.
# Press Alt-g anywhere; press q inside lazygit to close and return.
# In an SSH pane the local tmux still owns the key, so instead of a local popup
# we type lazygit into that pane to run it ON the remote (in its actual cwd).
bind -n M-g if-shell -F '#{m:*ssh*,#{pane_current_command}}' 'send-keys "lazygit" Enter' 'display-popup -E -w 90% -h 90% -d "#{pane_current_path}" lazygit'

# Floating yazi popup scoped to the current pane's directory.
# Press Alt-y anywhere; press q inside yazi to close and return.
bind -n M-y display-popup -E -w 90% -h 90% -d "#{pane_current_path}" yazi

# ----------------------------------------------------------------------------
